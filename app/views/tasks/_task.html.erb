<div class="media">
  <ol style="list-style-type: none" class="media list">
    <% @tasks.each do |task| %>
    <li>
      <h2><%= task.description %></h2>
      <small>Expires: <%= task.expires_at %></small>
    </li>
    <% end %>
  </ol>
</div>
